replicaCount: 1

config:
  READY_REQUIRE_CACHE: "true"

externalSecret:
  enabled: true
  secretStoreRef:
    kind: ClusterSecretStore
    name: postal-converter-ja-stg
  data:
    POSTGRES_DATABASE_URL:
      key: postal-converter-ja/stg/database
      property: postgres_url
    MYSQL_DATABASE_URL:
      key: postal-converter-ja/stg/database
      property: mysql_url
    SQLITE_DATABASE_PATH:
      key: postal-converter-ja/stg/database
      property: sqlite_path
    REDIS_URL:
      key: postal-converter-ja/stg/cache
      property: redis_url

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: postal-converter-ja-stg.example.com
      paths:
        - path: /
          pathType: Prefix

networkPolicy:
  enabled: true
  ingress:
    from:
      - namespaceSelector:
          matchLabels:
            kubernetes.io/metadata.name: ingress-nginx
