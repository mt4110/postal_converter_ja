replicaCount: 1

externalSecret:
  enabled: true
  secretStoreRef:
    kind: ClusterSecretStore
    name: postal-converter-ja-dev
  data:
    POSTGRES_DATABASE_URL:
      key: postal-converter-ja/dev/database
      property: postgres_url
    MYSQL_DATABASE_URL:
      key: postal-converter-ja/dev/database
      property: mysql_url
    SQLITE_DATABASE_PATH:
      key: postal-converter-ja/dev/database
      property: sqlite_path
    REDIS_URL:
      key: postal-converter-ja/dev/cache
      property: redis_url

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: postal-converter-ja-dev.example.com
      paths:
        - path: /
          pathType: Prefix

networkPolicy:
  enabled: true
  ingress:
    from:
      - namespaceSelector:
          matchLabels:
            kubernetes.io/metadata.name: ingress-nginx
